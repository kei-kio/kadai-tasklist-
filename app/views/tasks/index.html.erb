<h1>タスク一覧</h1>
<%= link_to 'ログアウト', logout_path, method: :delete %></li>

<% if logged_in? %>
  <%= render "tasks",tasks: @tasks %>

  <%= link_to "新規タスク作成", new_task_path %>
  
<% else %>
  
  <%= render "sessions/new" %>
<% end %>
