<h1>id: <%= @task.id %> の編集</h1>

<%= form_with(model: @task,local: true) do |f| %>
  <%= f.label :content,"タスク" %>
  <%= f.text_field :content %>
  
  <%= f.submit "作成" %>
<% end %>

<%= link_to "一覧へもどる",tasks_path %>